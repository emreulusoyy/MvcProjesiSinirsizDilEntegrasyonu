@using DtoLayer.Web.Layout;

@model NavbarViewModel

<header id="site-header" class="fixed-top">
    <div class="container">
        <nav class="navbar navbar-expand-lg navbar-dark stroke">
            <h1>
                <a class="navbar-brand" href="/anasayfa/index/">
                    <img src="~/logo.jpeg" style="height:70px;" />  Yılkar2 Gıda
                </a>
            </h1>
            <!-- if logo is image enable this
            <a class="navbar-brand" href="#index.html">
                <img src="image-path" alt="Your logo" title="Your logo" style="height:35px;" />
            </a> -->
            <button class="navbar-toggler  collapsed bg-gradient" type="button" data-toggle="collapse"
                    data-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false"
                    aria-label="Toggle navigation">
                <span class="navbar-toggler-icon fa icon-expand fa-bars"></span>
                <span class="navbar-toggler-icon fa icon-close fa-times"></span>

            </button>
            <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
                <ul class="navbar-nav ml-lg-auto">
                    <li class="nav-item active">
                        <a class="nav-link" href="/Anasayfa/index/@ViewBag.LanguageID">@Model.HomePage <span class="sr-only">(current)</span></a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/Hakkinda/index/@ViewBag.LanguageID">@Model.AboutPage</a>
                    </li>
                    @if (Model.Category1.Status)
                    {
                        <li class="nav-item">
                            <form method="get" asp-action="Index" asp-controller="Urunler">
                                <input hidden name="LanguageID" value="@ViewBag.LanguageID">
                                <input hidden name="CategoryID" value="@Model.Category1.CategoryID">
                                <button style="background-color:transparent; border:0px" class="nav-link"><a >@Model.Category1.Name</a></button>
                            </form>
                        </li>
                    }
                    @if (Model.Category2.Status)
                    {
                        <li class="nav-item">
                            <form method="get" asp-action="Index" asp-controller="Urunler">
                                <input hidden name="LanguageID" value="@ViewBag.LanguageID">
                                <input hidden name="CategoryID" value="@Model.Category2.CategoryID">
                                <button style="background-color:transparent; border:0px" class="nav-link"><a>@Model.Category2.Name</a></button>
                            </form>
                        </li>
                    }
                    @if (Model.Category3.Status)
                    {
                        <li class="nav-item">
                            <form method="get" asp-action="Index" asp-controller="Urunler">
                                <input hidden name="LanguageID" value="@ViewBag.LanguageID">
                                <input hidden name="CategoryID" value="@Model.Category3.CategoryID">
                                <button style="background-color:transparent; border:0px" class="nav-link"><a>@Model.Category3.Name</a></button>
                            </form>
                        </li>
                    }
                    @if (Model.Category4.Status)
                    {
                        <li class="nav-item">
                            <form method="get" asp-action="Index" asp-controller="Urunler">
                                <input hidden name="LanguageID" value="@ViewBag.LanguageID">
                                <input hidden name="CategoryID" value="@Model.Category4.CategoryID">
                                <button style="background-color:transparent; border:0px" class="nav-link"><a>@Model.Category4.Name</a></button>
                            </form>
                        </li>
                    }
                    @if (Model.Category5.Status)
                    {
                        <li class="nav-item">
                            <form method="get" asp-action="Index" asp-controller="Urunler">
                                <input hidden name="LanguageID" value="@ViewBag.LanguageID">
                                <input hidden name="CategoryID" value="@Model.Category5.CategoryID">
                                <button style="background-color:transparent; border:0px" class="nav-link"><a>@Model.Category5.Name</a></button>
                            </form>
                        </li>
                    }


                    <li class="nav-item mr-lg-1">
                        <a class="nav-link" href="/Iletısım/index/@ViewBag.LanguageID">@Model.ContactPage</a>

                    </li>



                    <!--/search-right-->
                    @*<li class="header-search mr-lg-2 mt-lg-0 mt-4 position-relative">
                    <div class="search-right">
                    <a href="#search" class="btn btn-style search-btn" title="search">
                    <span class="fas fa-search mr-2" aria-hidden="true"></span>
                    </a>

                    </div>
                    </li>*@
                    <!--//search-right-->

                </ul>
            </div>


            <!-- toggle switch for light and dark theme -->
            <div class="mobile-position">
                <nav class="navigation">
                    <div class="theme-switch-wrapper">
                        <label class="theme-switch" for="checkbox">
                            <input type="checkbox" id="checkbox">
                            <div class="mode-container py-1">
                                <i class="gg-sun"></i>
                                <i class="gg-moon"></i>
                            </div>
                        </label>
                    </div>
                </nav>
            </div>
            <div class="dropdown" style="position:fixed; right:50px">
                <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    TR
                </button>
                <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                    @foreach (var language in Model.Languages)
                    {
                        <a href="@ViewBag.Link/@language.LangaugeID" class="dropdown-item">@language.LanguageCode</a>

                    }
                </div>
            </div>
            <!-- //toggle switch for light and dark theme -->
        </nav>
    </div>

</header>

